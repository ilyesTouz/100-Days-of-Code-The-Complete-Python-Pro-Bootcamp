# Create a Question class with an init() with two attributes : text and answer
class Question:
    
    # Initialize attributes
    def __init__(self, text, answer) -> None:
        self.text = text
        self.answer = answer

